
import { createAttachment } from '../parser.js';

createAttachment({
	type: 'pgn', emoji: ':clipboard:',
	description: 'Creates a preview of a PGN file.',
	execute: (message, attachment) => {
		
	}
});
